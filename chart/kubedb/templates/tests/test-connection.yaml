apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kubedb.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "kubedb.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    helm.sh/chart: {{ include "kubedb.chart" . }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
  - name: wget
    image: busybox
    command: ['wget']
    args:  ['-qO-', '{{ include "kubedb.fullname" . }}:{{ .Values.service.port }}{{ .Values.metricsPath }}']
  restartPolicy: Never
